# Pavillion Config

Ansible collection for pavillion personal server configuration.

Usage:
- Run `setup.sh` in to create the ansible virtual environment.
- Create `~/.passfiles/ansible_pavillion` with ansible vault password.
- Run `make`.
